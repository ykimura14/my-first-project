## my-first-project 共有セットアップ

このリポジトリは 3 台のパソコンで共有するために作成されています。

### 使い方
1. GitHub からクローン: `git clone https://github.com/ykimura14/my-first-project.git`
2. 作業を行う
3. 変更を保存する
4. `git add .` → `git commit -m "Update project files"` → `git push`
5. 他の PC では `git pull` で最新を取得
